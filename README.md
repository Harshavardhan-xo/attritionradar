# AttritionRadar
### Employee Attrition & Workforce Cost Dashboard

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

The dashboard generates synthetic workforce data, compares logistic regression and random forest attrition scoring, and estimates workforce cost exposure. Cost assumptions are configurable assumptions, not guarantees.